import React from "react";
import styles from "./Footer.less";

const Footer = () => {
  return (
    <div
      style={{
        height: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      @author hanzc
    </div>
  );
};

export default Footer;
